[project]
name = "dd"
version = "0.1.0"
description = "Local project dashboard data collector and static UI"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
  "ruff>=0.11.13,<0.12",
]

[tool.uv]
package = false
